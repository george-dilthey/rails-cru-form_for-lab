<h1>Songs</h1>

<ul>

    <%@songs.each do |s|%>
    <li><p><%=link_to s.name, song_path(s)%></p></li>
    <%end%>

</ul>

<%=link_to "New Song", new_song_path%> | <%=link_to "View Genres", genres_path%> | <%=link_to "View Artists", artists_path%>