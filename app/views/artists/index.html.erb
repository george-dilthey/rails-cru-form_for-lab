<h1>Artists</h1>

<ul>

    <%@artists.each do |a|%>
    <li><%=link_to a.name, artist_path(a)%></li>
    <%end%>

</ul>

<%=link_to "New Artist", new_artist_path%> | <%=link_to "View Songs", songs_path%> | <%=link_to "View Genres", genres_path%>